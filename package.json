{
  "name": "oklm-menubar",
  "version": "1.0.0",
  "description": "Tray app for macOS allowing to listen to OKLM radio on desktop with media keys support",
  "dependencies": {
    "electron-shortcut": "^0.4.1",
    "menubar": "*"
  },
  "devDependencies": {
    "electron-packager": "^5.1.0",
    "electron-prebuilt": "^0.36.0"
  },
  "main": "main.js",
  "scripts": {
    "build": "electron-packager . OKLM --platform=darwin --arch=x64 --version=0.36.0 --icon=img/Icon.icns",
    "start": "electron ."
  }
}
